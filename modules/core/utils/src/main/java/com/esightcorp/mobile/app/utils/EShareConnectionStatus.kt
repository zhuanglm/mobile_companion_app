package com.esightcorp.mobile.app.utils

enum class EShareConnectionStatus {
    Unknown,

    Connected,
    Disconnected,
    Initiated,
    ReceivedUserRejection,
    Timeout,
    AddressNotAvailable,
    IpNotReachable,
    Busy,
}
